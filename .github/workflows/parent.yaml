name: Main CI

on:
  push:
    branches: [ main ]

jobs:
  call-reusable:
    uses: Akashsami4456/test-repo-gha-ak/.github/workflows/reusable-ci.yml@main
    with:
      node-version: '18'
    secrets:
      token: ${{ secrets.MY_PAT }}
